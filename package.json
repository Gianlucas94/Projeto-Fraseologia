{
  "name": "fraseologia4M",
  "version": "1.0.0",
  "description": "Fraseologia4M",
  "author": "Gianlucas Almeida",
  "license": "MIT",
  "private": true,
  "build": "npm run generate",
  "scripts": {
    "dev": "npm",
    "build": "npm run generate",
    "start": "npm run start",
    "deploy": "npm run generate && surge --project dist/ --domain fraseologia4M.surge.sh"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "node-sass": "^7.0.1",
    "nuxt": "^2.0.0",
    "sass-loader": "^12.4.0",
    "uikit": "^3.10.1",
    "vue-date-pick": "^1.2.1",
    "vue-i18n": "^8.27.0",
    "vue-select": "^3.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.11.0"
  },
  "prettier": {
    "singleQuote": true
  }
}
